import './Modal.scss';
    export default () => {
        
        if (!isOpen) {
            return null; // Если isOpen false, то модальное окно не рендерится
        }

        return (
        <div className='Modal'>
            sdfsdfsdf
        </div>
    )}